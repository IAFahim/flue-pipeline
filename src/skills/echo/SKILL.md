---
name: echo
description: Repeat the input back, prefixed with ECHO.
---

Read `input` from the arguments.
Return a structured result with one field, `output`, containing the text "ECHO: " followed by the input value.
