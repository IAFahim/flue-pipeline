---
name: outline
description: Turn a raw topic or rough text into a structured outline.
---

Read `input` from the arguments.
Produce a short hierarchical outline of the main points, in the same language as the input.
Return a structured result with one field, `output`, containing the outline as plain text.
